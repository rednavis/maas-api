rootProject.name = 'maas-api'
includeBuild('maas-shared') {
    dependencySubstitution {
        substitute module('com.rednavis:maas-shared') with project(':')
    }
}
include 'api'
include 'core'
include 'auth'
include 'database'

